<div class="container">
    <div class="d-flex justify-content-center">
            <a href="{{ route('categoria.listar') }}" class="btn btn-success">Categoria</a>
            <a href="{{ route('produto.listar') }}" class="btn btn-success">Produtos</a>
        </div>
    </div>
</div>